# YuriRTC requests 16 MiB receive/send buffers on each shared WebRTC UDP socket.
# Linux silently caps SO_RCVBUF/SO_SNDBUF to these host-wide maxima, so keep the
# kernel ceiling in sync with content-node's udpSocketBuffer setting.
net.core.rmem_max = 16777216
net.core.wmem_max = 16777216
